{
  "functions": {
    "source": "backend",
    "runtime": "nodejs20",
    "predeploy": ["npm --prefix backend run build"]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
